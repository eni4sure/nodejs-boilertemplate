# Scripts Runner Docs

This folder contains scripts that can be run with `yarn run:script` command.

These scripts can be used to perfom database migrations & upkeep scripts.

### To run a script:

```bash
yarn run:script scripts/{path-to-script}
```